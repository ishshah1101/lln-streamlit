# LLN Streamlit Demo

A small Streamlit app demonstrating the Law of Large Numbers (LLN): as sample size increases, the sample mean approaches the true mean.

## Run locally
```bash
pip install -r requirements.txt
streamlit run app.py
